[package]
version = "0.1.0"
name = "espresso-derivation"
edition = "2021"

[dependencies]
alloy-sol-types = "0.7.2"
sp1-zkvm = { git = "https://github.com/succinctlabs/sp1.git", tag = "v1.0.7-testnet" }
espresso-derivation-utils = { path = "../utils" }
